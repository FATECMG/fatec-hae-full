/**
 * An interface that defines the properties of a filter. Filters are used to query entities.
 * It must be extended by all filters.
 * @interface
 */
export interface Filter {
  active?: boolean
}
