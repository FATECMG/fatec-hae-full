/**
 * A constant object that maps field names to it corresponding portuguese translation.
 * Used to generate error messages for duplicated fields in MongoDB repositories.
 */
export const errorLabel = {
  registerNumber: 'Número de registro',
  email: 'E-mail'
}
